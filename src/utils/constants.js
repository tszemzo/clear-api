const CLIENT_TYPE = Object.freeze({
  PRIVATE: 'private',
  PUBLIC: 'public',
});

const MAX_NOTES = 30;
const MAX_NOTE_LENGTH = 1000;

module.exports = {
  CLIENT_TYPE,
  MAX_NOTES,
  MAX_NOTE_LENGTH
};
